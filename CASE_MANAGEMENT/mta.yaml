_schema-version: 3.3.0
ID: CASE_MANAGEMENT
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: CASE_MANAGEMENT-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      memory: 256M
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: CASE-db
      - name: CASE_MANAGEMENT-auth

  - name: CASE_MANAGEMENT-db-deployer
    type: hdb
    path: db #gen/db
    parameters:
      buildpack: nodejs_buildpack
      memory: 100M
    requires:
      - name: CASE-db

resources:
  - name: CASE_MANAGEMENT-auth
    type: com.sap.xs.hdi-container
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: CASE_MANAGEMENT-${org}-${space}
        tenant-mode: dedicated
  - name: CASE-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: CASE_MANAGEMENT-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: CASE_MANAGEMENT-${org}-${space}
        tenant-mode: dedicated
